| Source | Purpose | Concrete Referent | Role | Ordering | Candidate Term | First-Use Definition |
| --- | --- | --- | --- | --- | --- | --- |
| Review finding and `deepwell/src/services/render/literal_regions/wikidot.rs` | Record why Deepwell treats a complete closer-free Rate head differently while pairing Wikidot conditionals | The conditional literal-range rule that recognizes only a complete module head whose first name is ASCII-case-insensitive `Rate` and prevents an absent module closer from masking later `iftags` boundaries through EOF | Means | required BND-10 deviation note, file:line, narrow bare-Rate EOF rationale, public real-theme test controls, no 301 lineage | bare-Rate conditional-boundary exception | The bare-Rate conditional-boundary exception refers to the bounded Deepwell rule that keeps later `iftags` boundaries outside literal ownership when a complete Rate head has no module closer. |
| `deepwell/seeder/theme-basalt.ftml` and the existing Rate specification | Identify the existing repository evidence exercised through the public render boundary | The canonical Basalt source containing a closer-free Rate head inside a root `iftags +theme` gate, rendered once with the `theme` tag and once without it, plus a literal-owner control | Record | required BND-10 deviation note, file:line, narrow bare-Rate EOF rationale, public real-theme test controls, no 301 lineage | public Basalt render regression | The public Basalt render regression refers to the `RenderService::render` integration case that checks active, inactive, and literal-owner behavior using repository source and established page tags. |
| `docs/ftml-boundary.md` BND-10 | State when Deepwell can remove this source-level conditional pairing rule | An FTML interface that preserves `iftags` and Rate module boundaries as delayed structured nodes, including closer-free and paired Rate forms, so Deepwell supplies page tags and runtime module output without rescanning source | State | required BND-10 deviation note, file:line, narrow bare-Rate EOF rationale, public real-theme test controls, no 301 lineage | delayed conditional and Rate ownership | Delayed conditional and Rate ownership refers to FTML preserving the relevant conditional and module boundaries until Wikijump resolves their runtime state. |
