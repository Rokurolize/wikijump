| Source | Purpose | Concrete Referent | Role | Ordering | Candidate Term | First-Use Definition |
| --- | --- | --- | --- | --- | --- | --- |
| Requested `file.create` audit contract | Record successful file creation | The audit-log row inserted after the file's first revision is created, in the same transaction, with actor, site, page, file, first-revision, and supplied-IP identity | Event | emit after first revision in same creation transaction | `file.create` / `FileCreate` | `file.create` refers to the audit event recording successful creation of a file and its first revision. |
| Requested nested rollback proof | Describe the database atomicity boundary without implying object-store rollback | The enclosing transaction rollback that removes the newly inserted file, first revision, and audit-log rows | Event | nested rollback proves file+revision+audit only | file-creation database rollback | A file-creation database rollback refers to rollback of the file, first-revision, and audit rows created by the nested operation. |
