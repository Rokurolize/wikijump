| Source | Purpose | Concrete Referent | Role | Ordering | Candidate Term | First-Use Definition |
| --- | --- | --- | --- | --- | --- | --- |
| Production calls to `Router::route` in `wws/src/route.rs` | Represent each source registration exactly once | One production route call that binds one literal path to one method router and its handler | Record | discover each production route call, resolve its method router and handler, then reject omissions or duplicates | WWS route registration | A WWS route registration refers to one production `Router::route` call that binds one literal path to one method router and handler. |
| The complete set of production `Router::route` calls whose captured bytes match committed Git blobs | Define the fixed input population for route-attribution work | The 30 WWS route registrations in the committed route-registry blob, including the default-code registration and both resized-image registrations | Value | capture source bytes, require their committed Git blobs, discover all registrations, then compare the complete set | WWS route registration denominator | The WWS route registration denominator refers to the complete set of production WWS route registrations in the committed source-input blobs. |
| Git blob and byte hash for every captured source file used to resolve registrations and handlers | Make the denominator auditable without embedding the commit that contains it | The Git blob ID and SHA-256 calculated from the exact captured bytes parsed by one generated denominator | Record | capture each input once, hash those bytes, require the matching committed Git blob, then emit the denominator | Source identity record | A source identity record refers to the Git blob and SHA-256 of the exact captured source bytes used to generate the denominator. |
