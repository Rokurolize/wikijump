| Source | Purpose | Concrete Referent | Role | Ordering | Candidate Term | First-Use Definition |
| --- | --- | --- | --- | --- | --- | --- |
| Production calls to `Router::route` in `wws/src/route.rs` | Represent each source registration exactly once | One production route call that binds one literal path to one method router and its handler | Record | discover each production route call, resolve its method router and handler, then reject omissions or duplicates | WWS route registration | A WWS route registration refers to one production `Router::route` call that binds one literal path to one method router and handler. |
| The complete set of production `Router::route` calls at one repository identity | Define the fixed input population for route-attribution work | The 30 WWS route registrations present at the pinned Git commit and tree, including the default-code registration and both resized-image registrations | Value | pin the repository identity, discover all registrations, then compare the complete set | WWS route registration denominator | The WWS route registration denominator refers to the complete set of production WWS route registrations at the pinned repository identity. |
| Git identity and byte hashes for every source file used to resolve registrations and handlers | Make the denominator auditable against immutable inputs | The pinned commit, tree, route-registry SHA-256, and handler-file SHA-256 values used by one generated denominator | Record | pin repository identity, hash every input, then emit the denominator | Source identity record | A source identity record refers to the Git identity and SHA-256 values of every source input used to generate the denominator. |
