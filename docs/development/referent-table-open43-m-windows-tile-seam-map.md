| Source | Purpose | Concrete Referent | Role | Ordering | Candidate Term | First-Use Definition |
| --- | --- | --- | --- | --- | --- | --- |
| GitHub issue #756 and `M756_WINDOWS_TILE_DECLARATION` | Identify the public document output that must be reproduced | The exact `meta` element that a live Wikidot page emits when its Windows 8 tile image is configured | Value | exact live head declaration, then site-owned local route, then focused SSR/route test | Windows tile head declaration | Windows tile head declaration refers to the exact `meta` element emitted by a configured live Wikidot page. |
| GitHub issue #756 and `M756_WINDOWS_TILE_DECLARATION` | Identify the browser request controlled by the configured site setting | The exact site-local path referenced by the live Windows tile head declaration and the HTTP response produced for that path | Value | exact live head declaration, then site-owned local route, then focused SSR/route test | Windows tile route response | Windows tile route response refers to the response produced for the exact site-local path named by the live declaration. |
| `E_ICON_PANEL` and `E_ICON_OBSERVATIONS` | Separate stored configuration evidence from public output evidence | The observed `ManageSiteWindowsIconModule` upload controls, which prove that a Windows tile image slot exists but do not record its public declaration or route | Record | read admin evidence, then compare public head evidence, then decide whether implementation is supported | Windows tile control evidence | Windows tile control evidence refers to the frozen admin observations proving that the setting exists. |
| `docs/development/open43-m-closure-audit.json` | Prevent inferred HTML and route behavior from being classified as source-ready | The absence of a frozen or independently captured configured public page that reveals both the exact head element and its site-local path | State | read admin evidence, then compare public head evidence, then decide whether implementation is supported | Public declaration evidence gap | Public declaration evidence gap refers to the missing configured live-page observation needed to select exact SSR and route expectations. |
