| Source | Purpose | Concrete Referent | Role | Ordering | Candidate Term | First-Use Definition |
| --- | --- | --- | --- | --- | --- | --- |
| Axum `get(handler)` and `on(MethodFilter::GET, handler)` route declarations | Represent each fixed request-method slot exposed by a WWS path | A request whose method is GET is dispatched to the route's registered GET handler | Record | fixed method slot, then implicit HEAD, then unmatched-method handler | GET | GET refers to the WWS handler selected for an HTTP GET request on one route path. |
| Axum GET method routing | Represent Axum's automatic HEAD-to-GET dispatch without inventing a separate source handler | A request whose method is HEAD is dispatched through the route's GET handler and has its response body removed | Record | fixed method slot, then implicit HEAD, then unmatched-method handler | HEAD | HEAD refers to the implicit WWS HEAD surface supplied by a route's GET handler. |
| Standalone Axum `any(handler)` route declaration | Represent a route with no fixed method slot whose one handler receives every request method | Any request method on that path is dispatched to the same handler | Record | all-method handler with no fixed method slots | ANY | ANY refers to one WWS handler that receives every HTTP method for a route path. |
| Axum `MethodRouter::fallback(handler)` or a same-path `get(handler)` merged with `any(handler)` | Represent the handler selected only after no fixed request-method slot matches | A request method not claimed by the route's fixed method slots is dispatched to the route-local unmatched-method handler | Record | fixed method slot, then implicit HEAD, then unmatched-method handler | FALLBACK | FALLBACK refers to the WWS handler selected only when no fixed method slot on the same route path matches. |
