| Source | Purpose | Concrete Referent | Role | Ordering | Candidate Term | First-Use Definition |
| --- | --- | --- | --- | --- | --- | --- |
| wikidot.py `history/PageRevisionListModule` contract | Expose the caller's exact page-scoped history read through Deepwell | A public read that accepts a site ID, page ID, newest-through-oldest selection, and bounded result count, checks page visibility, then returns typed revisions | Means | page and site permission checks before revision selection | `page_revision_range` | `page_revision_range` refers to the existing page-scoped, permission-checked revision range read. |
| wikidot.py `history/PageSourceModule` and `history/PageVersionModule` contracts | Resolve a caller-supplied Wikidot revision ID without exposing another site or hidden page | A public read that accepts a site ID and revision ID, resolves only a same-site page coordinate, checks page visibility, then returns the selected typed revision details | Means | site-constrained coordinate resolution, then page permission, then revision detail selection | `page_revision_get_by_id` | `page_revision_get_by_id` refers to the site-constrained, permission-checked revision detail read keyed by revision ID. |
| Wikidot AMC response fixtures and current wikidot.py parsers | Preserve the response HTML that the unmodified client consumes | Adapters that render typed Deepwell revision records as `table.page-history`, `div.page-source`, or historical compiled body HTML | Means | typed Deepwell output before Wikidot-compatible HTML | `renderWikidotPageRevision*` | `renderWikidotPageRevision*` refers to the three Framerail adapters for the proven Wikidot history response DOM shapes. |
