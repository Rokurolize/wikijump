| Source | Purpose | Concrete Referent | Role | Ordering | Candidate Term | First-Use Definition |
| --- | --- | --- | --- | --- | --- | --- |
| Historical compatibility archive | Keep a retained campaign archive inspectable without making that archive current execution authority | The Git-tracked reconciler inventories an operator-supplied archive root, combines filesystem/process/Docker/worktree/archive-control/repository-citation inputs, and writes a generated catalog | Means | explicit archive root -> tracked reconciliation logic -> generated catalog | wjlab reconciler | Wjlab reconciler refers to the Git-tracked inventory program that derives retention facts for an explicitly selected historical archive without mutating it. |
| Existing release policy | Preserve safety rules independently of mutable lab contents | The Git-tracked JSON registry containing protected-resource rules, artifact selectors, release conditions, and cleanup methods for wjlab resources | Record | retention decision -> release rules -> cleanup admission | wjlab release registry | Wjlab release registry refers to the version-controlled rules used by the reconciler when evaluating artifact cleanup eligibility. |
| Historical cleanup request | Separate obsolete control material from the retained archive inventory | `<archive-root>/trash/`, whose contents are retained history but excluded from archive control-file scanning and active catalog entries | State | classify obsolete control material -> move to trash -> exclude from archive catalog | non-operative trash | Non-operative trash refers to retained campaign history that no longer participates in archive retention references. |
| Repository evidence records | Prevent archive cleanup from overlooking durable historical citations | Legacy `/home/roku/wjlab/...` provenance strings found in Git-tracked records are remapped against the explicitly supplied archive root before retention evaluation | Record | scan repository -> remap legacy provenance -> retain cited archive artifact | repository citation | Repository citation refers to a historical Git-tracked provenance reference used only when reconciling an explicitly selected archive; it is not a development/runtime dependency. |
