{
  "schema": "roku.wjlab.release_conditions.v1",
  "policy": {
    "default": "unresolved artifacts remain stored",
    "release_requires_all_conditions": true,
    "freshness": "process, lease, Docker, Git, receipt, and runtime checks must be repeated immediately before cleanup",
    "session_history_role": "session history establishes provenance and intended lifetime but cannot override current authoritative references"
  },
  "rules": [
    {
      "id": "protected-standing-volumes",
      "selectors": {"docker_volume_names": ["runtime50x-postgres-data", "runtime50x-files-data"]},
      "artifact_kind": "protected runtime data",
      "retention_blockers": ["repository policy requires explicit user authorization"],
      "release_conditions": [
        {"requirement": "the user explicitly authorizes deletion of the exact named volume after being told the data and recovery consequences", "check": "record the authorization and exact volume name in the reclamation receipt"},
        {"requirement": "no standing, candidate, rollback, migration, or recovery process references the volume", "check": "docker inspect every container and volume consumer"},
        {"requirement": "a verified backup or an explicit decision to abandon recovery exists", "check": "verify the backup receipt and restore procedure, or record the abandonment decision"}
      ],
      "cleanup_method": "explicit exact-volume removal only",
      "automatic_release": false
    },
    {
      "id": "registered-worktree",
      "selectors": {"path_globs": ["/home/roku/wjlab/worktrees/**", "/home/roku/wjlab/ftml-marker-*-work*/**"]},
      "artifact_kind": "Git worktree",
      "retention_blockers": ["dirty or untracked changes", "unreachable commits", "Git worktree lock", "process or Docker reference", "receipt-cited path"],
      "release_conditions": [
        {"requirement": "tracked and untracked changes are empty or durably integrated elsewhere", "check": "git status --porcelain=v1 and, when nonempty, patch/commit equivalence verification"},
        {"requirement": "HEAD is reachable from the intended merged branch or its patch is proven equivalent to an integrated commit", "check": "git merge-base --is-ancestor, or patch-id/tree comparison with a recorded destination commit"},
        {"requirement": "the worktree is unlocked or its recorded owner and expiry have become terminal", "check": "git worktree list --porcelain and the owner campaign receipt"},
        {"requirement": "no process, container, standing bind mount, receipt, or rollback refers to the path", "check": "scan /proc, docker inspect including stopped containers, and explicit receipt/index paths"}
      ],
      "cleanup_method": "git -C <main-repository> worktree remove <exact-path>; branch deletion is a separate decision",
      "automatic_release": true
    },
    {
      "id": "cargo-target",
      "selectors": {"path_globs": ["/home/roku/wjlab/cargo-targets/**", "/home/roku/wjlab/targets/**", "/home/roku/wjlab/candidates/*/target", "/home/roku/wjlab/worktrees/**/target"]},
      "artifact_kind": "reproducible Rust build output",
      "retention_blockers": ["active compiler or lease", "active candidate or rollback generation", "missing reconstruction proof", "receipt path reference"],
      "release_conditions": [
        {"requirement": "source commit, dependency lock, profile, features, flags, and toolchain or candidate manifest are recorded", "check": "verify the build receipt or candidate manifest and source reachability"},
        {"requirement": "the target is neither the active candidate nor the one retained rollback", "check": "compare current candidate, standing, and rollback receipts"},
        {"requirement": "no cargo, rustc, builder, lease, process, or container uses the exact target", "check": "resource-lease status, /proc path scan, process list, and docker inspect"},
        {"requirement": "no cited receipt requires the target path itself", "check": "scan explicit receipt, manifest, index, and verdict path fields"}
      ],
      "cleanup_method": "cargo clean --dry-run with the exact target, then cargo clean with the same target",
      "automatic_release": true
    },
    {
      "id": "candidate-runtime-directory",
      "selectors": {"path_globs": ["/home/roku/wjlab/runtime-candidates/**"]},
      "artifact_kind": "candidate runtime configuration",
      "retention_blockers": ["running or stopped containers", "active candidate", "retained rollback", "nonterminal candidate receipt", "browser evidence reference"],
      "release_conditions": [
        {"requirement": "the candidate receipt is terminal and the generation is older than both active and retained rollback", "check": "compare candidate run IDs, source identities, verdicts, and promotion receipts"},
        {"requirement": "all project containers are intentionally closed and no container metadata refers to the directory", "check": "docker ps -a and docker inspect by compose project and working directory"},
        {"requirement": "no browser process/profile, acceptance receipt, or rollback procedure references the generation", "check": "process scan and explicit receipt/index path scan"}
      ],
      "cleanup_method": "close the exact Compose project without volumes needed elsewhere, then remove the exact directory",
      "automatic_release": true
    },
    {
      "id": "candidate-artifact-directory",
      "selectors": {"path_globs": ["/home/roku/wjlab/candidates/**"]},
      "artifact_kind": "candidate binary, build target, deployment receipt, measurement result, or fixture bundle",
      "retention_blockers": ["active or rollback candidate", "process/container/lease reference", "receipt-cited acceptance descendant", "missing terminal receipt or reconstruction proof"],
      "release_conditions": [
        {"requirement": "the campaign and candidate are terminal and the generation is neither active nor immediate rollback", "check": "candidate, promotion, standing, and rollback receipts"},
        {"requirement": "no running or stopped container, process, mount, open descriptor, or lease references the path", "check": "process, /proc, Docker, and resource-lease scans"},
        {"requirement": "every cited verdict, classification, reconciliation, fixture, or measurement is retained at its registered absolute path", "check": "transitive explicit-path reference graph"},
        {"requirement": "build-only descendants have source, lock, profile, feature, recipe, toolchain, and digest reconstruction proof", "check": "candidate manifest and source reachability"}
      ],
      "cleanup_method": "clean exact Cargo targets through Cargo first; then remove only the released candidate child, preserving cited descendants",
      "automatic_release": true
    },
    {
      "id": "standing-runtime-generation",
      "selectors": {"path_globs": ["/home/roku/wjlab/runtime/wikijump-standing*"]},
      "artifact_kind": "standing runtime or standing rollback configuration",
      "retention_blockers": ["served standing identity", "immediate rollback generation", "container bind/config reference"],
      "release_conditions": [
        {"requirement": "a newer standing generation is healthy and a newer immediate rollback exists", "check": "standing refresh and health receipts identify all three generations"},
        {"requirement": "no running or stopped container refers to the old generation", "check": "docker inspect every standing project container"},
        {"requirement": "no cited standing receipt or recovery procedure requires its absolute path", "check": "explicit receipt/index path scan"}
      ],
      "cleanup_method": "remove only the superseded configuration generation; preserve shared standing volumes",
      "automatic_release": true
    },
    {
      "id": "standing-runtime-source",
      "selectors": {"path_globs": ["/home/roku/wjlab/runtime-sources/**"]},
      "artifact_kind": "standing source checkout",
      "retention_blockers": ["standing bind mount", "active or rollback source identity", "unreachable source commit", "dirty source"],
      "release_conditions": [
        {"requirement": "neither standing nor rollback containers bind the path", "check": "docker inspect all running and stopped standing containers"},
        {"requirement": "the source commit and dependency locks remain reachable and recorded", "check": "verify refresh receipt, Git reachability, and lock digests"},
        {"requirement": "the source tree is clean and no receipt requires the checkout path", "check": "git status and explicit receipt/index path scan"}
      ],
      "cleanup_method": "use git worktree removal when registered; otherwise remove the exact reproducible source clone",
      "automatic_release": true
    },
    {
      "id": "candidate-or-standing-image",
      "selectors": {"docker_label_globs": ["com.rokurolize.wikijump.lifecycle=*", "com.docker.compose.project=wikijump-*"]},
      "artifact_kind": "OCI image",
      "retention_blockers": ["container reference", "active or rollback digest", "nonterminal candidate", "missing reproducible build identity"],
      "release_conditions": [
        {"requirement": "no running or stopped container references the image ID or digest", "check": "docker ps -a plus docker inspect image references"},
        {"requirement": "the digest is not current standing, active candidate, immediate rollback, or a nonterminal receipt input", "check": "compare standing, candidate, rollback, and build receipts"},
        {"requirement": "source, lockfiles, recipe, base digests, platform, and build manifest remain recorded", "check": "verify provenance build manifest and source reachability"}
      ],
      "cleanup_method": "docker image rm by exact image ID after recording the retained manifests",
      "automatic_release": true
    },
    {
      "id": "docker-build-cache",
      "selectors": {"docker_resource_types": ["build-cache"]},
      "artifact_kind": "reproducible Docker builder cache",
      "retention_blockers": ["active build or exclusive heavy-work lease", "cache age below campaign threshold"],
      "release_conditions": [
        {"requirement": "no buildx/buildkit job or heavy-work lease is active", "check": "process list, buildx status, and resource-lease status"},
        {"requirement": "the selected records satisfy the campaign age/filter policy", "check": "docker buildx du and prune dry-run/filter output"}
      ],
      "cleanup_method": "docker buildx prune with an explicit age/filter; never combine with runtime-volume cleanup",
      "automatic_release": true
    },
    {
      "id": "structured-acceptance-evidence",
      "selectors": {"path_globs": ["/home/roku/wjlab/evidence/**", "/home/roku/wjlab/receipts/**", "/home/roku/wjlab/frozen/**"]},
      "artifact_kind": "receipt, verdict, classification, reconciliation, fixture, denominator, browser capture, or live reference",
      "retention_blockers": ["explicit citation from a current receipt/index/plan", "acceptance claim depends on the artifact", "artifact cannot be reconstructed from source revision"],
      "release_conditions": [
        {"requirement": "no current receipt, index, ledger, plan, PR evidence, or completion claim cites the artifact path", "check": "build the transitive explicit-path reference graph"},
        {"requirement": "no retained acceptance result depends on the artifact as its verdict, classification, reconciliation, or fixture", "check": "inspect the citing receipt's typed artifact role, not merely filename adjacency"},
        {"requirement": "a superseding sealed artifact preserves the same fixed inputs and denominator, or the campaign explicitly retires the claim", "check": "compare immutable input identities and record the supersession/retirement receipt"}
      ],
      "cleanup_method": "remove only exact released leaves; never delete a whole evidence family because one sibling is rebuildable",
      "automatic_release": true
    },
    {
      "id": "raw-runtime-or-build-log",
      "selectors": {"path_globs": ["/home/roku/wjlab/**/*.log", "/home/roku/wjlab/**/*.stdout", "/home/roku/wjlab/**/*.stderr", "/home/roku/wjlab/**/*.pid", "/home/roku/wjlab/**/*tail*"]},
      "artifact_kind": "raw stdout/stderr or build transcript",
      "retention_blockers": ["open file descriptor", "active failure diagnosis", "explicit receipt citation", "only available terminal result"],
      "release_conditions": [
        {"requirement": "the producing process is terminal and no process has the file open", "check": "session/process status and /proc file-descriptor scan"},
        {"requirement": "structured terminal receipt, failure classification, or success verdict has been retained", "check": "verify the producer's result artifact and identity"},
        {"requirement": "no receipt, index, PR, or unresolved diagnosis cites the exact log", "check": "explicit path-reference scan and creator-session search"}
      ],
      "cleanup_method": "remove the exact log while preserving structured evidence and its containing family",
      "automatic_release": true
    },
    {
      "id": "provenance-build-root",
      "selectors": {"path_globs": ["/home/roku/wjlab/evidence/standing-provenance-build-*", "/home/roku/wjlab/evidence/open87-pr1334-candidate-build-*"]},
      "artifact_kind": "candidate/standing provenance build evidence",
      "retention_blockers": ["active build", "nonterminal run", "active or rollback generation", "receipt-cited acceptance leaf", "missing terminal failure/success receipt"],
      "release_conditions": [
        {"requirement": "the build process and exclusive lease are terminal", "check": "process, /proc, buildx, and resource-lease status"},
        {"requirement": "the generation is older than active and retained rollback and no container/image lifecycle depends on it", "check": "candidate, standing, rollback, container, and image receipt comparison"},
        {"requirement": "terminal manifest, source/tree/lock/base identities, and failure or success receipt are retained", "check": "validate the build manifest and terminal receipt"},
        {"requirement": "every acceptance leaf explicitly cited outside the build root is retained or superseded", "check": "transitive explicit-path reference graph"}
      ],
      "cleanup_method": "retain cited leaves and terminal receipts; remove exact rebuildable build-output subtrees or a fully released root",
      "automatic_release": true
    },
    {
      "id": "incomplete-attempt",
      "selectors": {"path_globs": ["/home/roku/wjlab/**/*.incomplete", "/home/roku/wjlab/**/*-attempt*.incomplete"]},
      "artifact_kind": "failed or interrupted attempt",
      "retention_blockers": ["producer still active", "failure not classified", "attempt is the only evidence for an unresolved defect", "receipt citation"],
      "release_conditions": [
        {"requirement": "the producer is terminal and cannot resume this attempt", "check": "session, process, lease, and controller state"},
        {"requirement": "the failure is durably classified or superseded by a later successful attempt", "check": "failure receipt or later run receipt with matching intended inputs"},
        {"requirement": "no current investigation or receipt cites the exact attempt", "check": "session search and explicit path-reference graph"}
      ],
      "cleanup_method": "remove the exact attempt after retaining the compact failure classification",
      "automatic_release": true
    },
    {
      "id": "controller-or-build-context",
      "selectors": {"path_globs": ["/home/roku/wjlab/.standing-*-controller-exec/**", "/home/roku/wjlab/.standing-provenance-build-contexts/**"]},
      "artifact_kind": "ephemeral controller executable or Docker build context",
      "retention_blockers": ["active controller/build process", "open file descriptor", "missing terminal receipt"],
      "release_conditions": [
        {"requirement": "the owning run is terminal and its controller/build processes and lease are gone", "check": "run ID against process, /proc, buildx, and resource-lease status"},
        {"requirement": "the run's terminal receipt and effective context digest are stored outside the temporary context", "check": "verify the evidence manifest and digest fields"},
        {"requirement": "no receipt names the temporary path", "check": "explicit path-reference scan"}
      ],
      "cleanup_method": "remove the exact run-owned temporary directory",
      "automatic_release": true
    },
    {
      "id": "browser-profile-or-test-runtime",
      "selectors": {"path_globs": ["/home/roku/wjlab/**/*browser*profile*", "/home/roku/wjlab/runtime-probe-*", "/home/roku/wjlab/xmlrpc-*-preflight*"]},
      "artifact_kind": "browser profile or disposable test runtime",
      "retention_blockers": ["browser/test process active", "profile is needed for authentication continuity", "rollback or acceptance receipt reference"],
      "release_conditions": [
        {"requirement": "the owning test run is terminal and all browser/server processes are gone", "check": "process, CDP endpoint, socket, and /proc path scan"},
        {"requirement": "required screenshots, network captures, console results, and cleanup verdict are retained separately", "check": "validate the run receipt and artifact paths"},
        {"requirement": "no authentication handoff, retry, or receipt refers to the profile/runtime", "check": "session search and explicit path-reference graph"}
      ],
      "cleanup_method": "remove the exact run-owned profile/runtime after its cleanup verdict",
      "automatic_release": true
    },
    {
      "id": "frozen-denominator",
      "selectors": {"path_globs": ["/home/roku/wjlab/frozen/**"]},
      "artifact_kind": "fixed campaign input or denominator",
      "retention_blockers": ["current campaign or receipt uses its identity", "comparability depends on the fixed denominator"],
      "release_conditions": [
        {"requirement": "every campaign and acceptance receipt using the denominator is retired or migrated to a sealed successor", "check": "reference graph plus successor receipt with explicit denominator mapping"},
        {"requirement": "the successor preserves auditability and does not rewrite historical counts", "check": "validate old-to-new mapping, digests, and fixed identities"}
      ],
      "cleanup_method": "archive or remove only after all dependent claims are retired; ordinary phase cleanup does not release it",
      "automatic_release": false
    }
  ],
  "known_assets": [
    {
      "selector": "/home/roku/wjlab/evidence/listpages-final-standing-20260804-final",
      "provenance": {"thread_id": "019fb13c-424c-7180-b3c5-ac4c87a0dc36", "purpose": "sealed final ListPages standing acceptance root"},
      "observed_blockers": ["final receipt explicitly cites acceptance descendants by absolute path"],
      "release_when": ["all receipts, PR evidence, and completion claims citing the root or descendants are retired", "a sealed successor preserves or explicitly retires the fixed denominator and reference identities"],
      "granularity": "retain the whole root unless the complete typed citation graph proves a smaller self-contained retained set"
    },
    {
      "selector": "/home/roku/wjlab/evidence/listpages-final-20260803",
      "provenance": {"thread_id": "019fb13c-424c-7180-b3c5-ac4c87a0dc36", "purpose": "superseded ListPages candidate and reconciliation generation"},
      "observed_blockers": ["structured descendants may still be explicit inputs to later acceptance artifacts"],
      "release_when": ["the transitive path-reference graph has zero inbound current citations", "no process or open descriptor refers to it", "the final standing acceptance root remains complete"],
      "granularity": "whole root after zero citations; exact raw logs may be released earlier under raw-runtime-or-build-log"
    },
    {
      "selector": "/home/roku/wjlab/evidence/listpages-f8-20260806",
      "provenance": {"thread_id": "019fb13c-424c-7180-b3c5-ac4c87a0dc36", "purpose": "later residual/open-issues ListPages candidate, fixture, and acceptance work"},
      "observed_blockers": ["Open87/PR1334 campaign remains nonterminal", "current records cite fixtures inside the root"],
      "release_when": ["PR1334/Open87 is merged and standing-verified", "final reconciliation and exact fixture are sealed at registered absolute paths", "no retained receipt cites this generation", "active and rollback generations are selected elsewhere"],
      "granularity": "remove superseded replay/candidate subroots as units; preserve every cited fixture/verdict pair"
    },
    {
      "selector": "/home/roku/wjlab/evidence/syntax-differential-20260726",
      "provenance": {"thread_ids": ["019f9ce6-f3e4-7c80-bac0-97de640dcba2", "019f9cd2-1548-7721-a581-264f50d1511a", "019f9d1f-cb1e-7e40-a987-d8d3da5384bb"], "purpose": "live/reference observations, classifications, comparisons, and fixtures"},
      "observed_blockers": ["non-reconstructible live/reference evidence feeds later compatibility issues"],
      "release_when": ["all descendant issues and campaign receipts migrate citations to a sealed successor", "the successor records absolute paths and the same fixed denominator/reference identities", "no active session or process uses runtime-pages-v3"],
      "granularity": "retain cited observation, classification, comparison, and fixture sets together; release whole root only after zero inbound citations"
    },
    {
      "selector": "/home/roku/wjlab/evidence/wikijump-all-issues-20260807",
      "provenance": {"thread_id": "019fe327-9b5c-74a3-940c-b25a7cca6231", "purpose": "Open87 fixed issue ledger, candidate fixtures, and issue acceptance evidence"},
      "observed_blockers": ["PR1334/Open87 tracking closure is incomplete", "ListPages candidate manifests and fixtures are cited"],
      "release_when": ["all linked issues are terminal", "open PR count is zero", "final ledger has zero missing, duplicate, or failed rows", "merged develop equals verified standing identity", "final browser/runtime receipt and rollback are sealed", "successor receipts no longer cite this root"],
      "granularity": "candidate runtime/fixture lanes are units; retain classifications and exact fixtures while cited"
    },
    {
      "selector": "/home/roku/wjlab/evidence/performance-admission-cb70f866",
      "provenance": {"thread_id": "019fe327-9b5c-74a3-940c-b25a7cca6231", "purpose": "paused performance admission whose isolated snapshot failed source/destination byte-count validation"},
      "observed_blockers": ["small pause and environment-invalidity receipts must be preserved", "runner/container/lease state needs fresh verification"],
      "release_when": ["measurement runner, lease, containers, and open descriptors are absent", "pause, invalidity, and release-admission receipts are retained", "no receipt treats isolated-snapshot or postgres.dump as valid acceptance evidence"],
      "granularity": "release isolated-snapshot/postgres.dump as one invalid snapshot; preserve compact receipts"
    },
    {
      "selector": "/home/roku/wjlab/evidence/open87-pr1334-candidate-build-*",
      "provenance": {"thread_ids": ["019febc2-3f1c-7572-b816-e52627c12583", "019fedf9-7139-7510-aa9d-23169b0c141d"], "purpose": "revision-specific candidate/standing provenance build attempts"},
      "observed_blockers": ["current campaign build/promotion remains active", "active and immediate rollback attempts must be selected", "some prior roots are explicitly cited"],
      "release_when": ["attempt is terminal", "attempt is older than active and immediate rollback", "no controller, lease, buildx process, container, or image lifecycle refers to it", "terminal manifest and result receipt are preserved", "no external receipt cites an acceptance leaf inside it"],
      "granularity": "whole attempt root after retaining small terminal manifests/receipts when needed; incomplete attempts additionally require a durable failure classification"
    },
    {
      "selector": "/home/roku/wjlab/evidence/standing-provenance-build-*",
      "provenance": {"thread_id": "019f85eb-b05f-7fe1-8b4d-4ee8a6e60a00", "purpose": "historical standing provenance builds and build identities"},
      "observed_blockers": ["current standing and rollback digest mapping must be reconciled", "some manifest/verdict leaves are explicitly cited"],
      "release_when": ["a later healthy promotion receipt identifies production and replacement rollback", "root is neither named generation", "no container/image/process/lease refers to it", "cited terminal manifests and verdicts are retained or superseded"],
      "granularity": "rebuildable builder/context subtrees may be released before cited terminal leaves; otherwise whole old attempt"
    },
    {
      "selector": "/home/roku/wjlab/worktrees/open87-pr2/*",
      "provenance": {"thread_id": "019fe327-9b5c-74a3-940c-b25a7cca6231", "purpose": "isolated Open87 implementation lanes intended to converge into the integration branch"},
      "observed_blockers": ["some workers have dirty/untracked changes", "squash/cherry-pick equivalence is not proven for every clean worker", "standing-ed5c5b353 is a current standing bind source"],
      "release_when": ["status including untracked files is clean", "ancestry or patch equivalence proves integration", "Git lock is absent/terminal", "no standing/container/process/receipt path reference remains"],
      "granularity": "one exact worktree through git worktree remove; branch cleanup is separate"
    },
    {
      "selector": "/home/roku/wjlab/cargo-targets/wikijump-d65bf4ff5-release-20260806",
      "provenance": {"purpose": "revision-specific release Cargo target from the August 6 campaign"},
      "observed_blockers": ["the inventory records an explicit path reference to its Deepwell binary", "current active/rollback mapping needs reconciliation"],
      "release_when": ["the citing receipt no longer requires the binary path or migrates to a retained manifest/digest", "target is neither active nor rollback", "no process, builder, lease, container, or open descriptor uses it", "source, lock, profile, features, flags, and toolchain are recorded"],
      "granularity": "exact target via Cargo dry-run and Cargo clean"
    },
    {
      "selector": "/home/roku/wjlab/targets/wikijump-open87-wws",
      "provenance": {"thread_id": "019fe327-9b5c-74a3-940c-b25a7cca6231", "purpose": "Open87 WWS development/build target"},
      "observed_blockers": ["active campaign previously designated it the current WWS development target", "producing manifest has not yet been bound in this registry"],
      "release_when": ["Open87 WWS source is terminal and merged", "a current build/receipt identifies its replacement or records full reconstruction inputs", "no process, lease, container, or receipt path reference remains"],
      "granularity": "exact child using the producing tool's native clean operation; preserve targets/README.md"
    },
    {
      "selector": "/home/roku/wjlab/runtime-sources/standing-ba76e2e1",
      "provenance": {"purpose": "historical standing source generation"},
      "observed_blockers": ["historical standing/promotion receipts may cite the absolute checkout path"],
      "release_when": ["neither current standing nor immediate rollback uses its source identity", "zero running/stopped container bind or Compose references", "zero process/open-descriptor and receipt references", "source SHA and dependency locks remain reachable"],
      "granularity": "Git worktree removal if registered; otherwise exact reproducible clone"
    },
    {
      "selector": "/home/roku/wjlab/worktrees/wikijump/saved-page-runtime-diff-899",
      "provenance": {"purpose": "standing-runtime locales bind and saved-page differential checkout"},
      "observed_blockers": ["Git lock owner=standing-runtime, expiry=2026-08-26T00:37:42Z, locales-bind"],
      "release_when": ["lock owner campaign is terminal after expiry", "standing and rollback containers no longer bind it", "worktree is clean and HEAD remains reachable", "zero receipt/process references"],
      "granularity": "git worktree remove only after the standing-runtime lock is cleared"
    }
  ]
}
