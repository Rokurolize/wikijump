# Open43 settings browser candidate case referents

| Source | Purpose | Concrete Referent | Role | Ordering | Candidate Term | First-Use Definition |
| --- | --- | --- | --- | --- | --- | --- |
| Shared candidate runner and browser parity controls | Keep browser ownership inside the runner | The lazy runner-owned facility that launches the parity browser, creates candidate-only contexts with approved storage state, and closes every context and browser | Means | Before the first browser case and during runner cleanup | `candidate_browser_contexts` | `candidate_browser_contexts` refers to the runner-owned facility that creates controlled contexts without exposing its Browser. |
| Open43 temporal evidence contract | Preserve the first user-visible interval | The public DOM, response, resource, request, console, and screenshot observation taken immediately after DOMContentLoaded | Record | First observation in each temporal pair | `initial_observation` | `initial_observation` refers to the candidate artifact captured immediately after DOMContentLoaded. |
| Open43 temporal evidence contract | Preserve the completed user-visible interval | The public DOM, response, resource, request, console, and screenshot observation taken after the declared completion checks finish | Record | Second observation in each temporal pair | `settled_observation` | `settled_observation` refers to the separate candidate artifact captured after completion checks finish. |
| Public site and category JSON-RPC reads | Verify mutations and restore the task-owned candidate | The exact pre-run and post-mutation settings values and revisions returned through public reads | State | Before each mutation, after each mutation, and before cleanup completes | `public_settings_snapshot` | `public_settings_snapshot` refers to settings values and revisions observed through public reads. |
| Private candidate input and public JSON-RPC reads | Select the task-owned category transition without making it a universal standing identity | The site, two category, and two existing page identities supplied privately and matched exactly through public reads | Value | Before any setting mutation | `settings_fixture_identity` | `settings_fixture_identity` refers to the private task fixture whose hash is sealed and whose values are verified through public reads. |
| Direct public category-page navigation | Bind the target category theme independently of the client transition | The immediate and settled semantic snapshots and capture failures from the direct main-document navigation to the fixed transition-category page | Record | Before the same-document category transition | `direct_theme_target_observation` | `direct_theme_target_observation` refers to the target page's two direct-navigation semantic intervals and their public resource failures. |
| Public category page navigation | Prove that the category theme changes without a stale client frame | The same-document link navigation from the fixed default-category page to the fixed transition-category page, with separate client-immediate and client-settled screenshots, computed styles, and capture failures | Event | After both category themes are stored | `category_theme_transition` | `category_theme_transition` refers to the public client navigation whose two intervals expose the source and target category themes independently of the direct target load. |
| Exact initial main-document response | Bind CSP evidence to the response that produced the immediate observation | The SHA-256 of the CSP header from the exact initial main-document navigation response and whether that header contains the observed script nonce | Record | Before reloads and client navigation can produce another document response | `initial_navigation_csp_identity` | `initial_navigation_csp_identity` refers only to the hashed CSP header and nonce relationship from the response that produced the direct immediate observation. |
| Public Deepwell `session_get` calls | Bind private actor inputs to publicly admitted session identities | Exact administrator and non-administrator user IDs plus a null expired-session result, with no expired user ID retained | Record | Before the first settings read or mutation | `actor_session_identity` | `actor_session_identity` refers to the public session results that match the two active actor IDs and prove the expired token has no session. |
| Public toolbar DOM and toolbar action | Prove that a setting change does not leave a stale toolbar during same-document navigation | One disabled public document retained across the toolbar mutation, followed by separate client-immediate and client-settled enabled toolbar observations, plus disabled and enabled server-rendered states at each fixed viewport | State | Before the toolbar mutation, immediately after its same-document navigation, and after completion | `toolbar_setting_transition` | `toolbar_setting_transition` refers to the retained public document whose observed toolbar state crosses the public setting mutation. |
| Public Framerail analytics and general settings forms | Identify the visible stale-revision result without trusting a hardcoded visibility claim | The normalized public SvelteKit ActionResult error code and SHA-256 of the visible error message while the dialog is displayed | Record | After each stale form submission and before closing its error dialog | `visible_revision_conflict` | `visible_revision_conflict` refers to the public failure identity and displayed message captured while the stale-revision dialog is visible. |
| Public Framerail general settings form | Prove the visible settings lifecycle instead of substituting a direct request | The edit, observed stale-revision error, successful save, no-reload fresh-revision confirmation save, reload, and client-navigation observations driven through `#sm-general-form` | Event | After the administrator initial capture and before the settled capture | `general_settings_ui_lifecycle` | `general_settings_ui_lifecycle` refers to the public browser form sequence that exposes stale error, successful save, and applied invalidation states. |
| Public permission action and JSON-RPC read | Prove that every denied outcome preserves both values and revision | The exact site identity, settings values, and settings revision read after each fixed permission, origin, and stale-revision action | Record | Before the first matrix action and after every action | `revision_bound_permission_outcome` | `revision_bound_permission_outcome` refers to one action status plus the following public site read, including its settings revision. |
| Shared runner cleanup requirement | Prevent a passing receipt from hiding changed public settings or an authenticated browser | The runner first closes its browser owner, then obtains public proof through HTTP and JSON-RPC that every setting changed by the nine reversible cases equals its exact pre-run value | Record | After case execution and before any pass receipt | `settings_cleanup_proof` | `settings_cleanup_proof` refers to post-browser public reads that prove exact settings restoration without a page or allocator cleanup authority. |
