| Source | Purpose | Concrete Referent | Role | Ordering | Candidate Term | First-Use Definition |
| --- | --- | --- | --- | --- | --- | --- |
| Open43 candidate acceptance plan | Attach acceptance work to the exact candidate without owning its stack | One sealed, unexpired, non-standing candidate whose source, images, configuration, endpoint, and Compose project remain stable through cleanup | State | sealed candidate -> pre-run identity -> public cases -> cleanup -> post-run identity -> publication | candidate runtime | A candidate runtime refers to the sealed external non-standing Wikijump stack observed before and after one case run. |
| Open43 candidate acceptance plan | Put lifecycle guarantees behind one small interface | The module that validates the candidate, creates the run resource record, invokes one source-owned case set, forces cleanup, reconciles the denominator, and publishes terminal evidence | Means | sealed candidate -> pre-run identity -> public cases -> cleanup -> post-run identity -> publication | CandidateCaseRunner | CandidateCaseRunner refers to the module that owns candidate attachment, run lifecycle, reconciliation, and terminal publication. |
| Open43 candidate acceptance plan | Keep feature behavior source-owned without a command DSL or per-feature runner | One source module that fixes the case IDs and inputs, issues feature-specific public requests, verifies their observations, and removes its run-owned resources through public requests | Record | fixed denominator -> fixed inputs -> public requests -> public verification -> public cleanup | CandidateCaseSet | CandidateCaseSet refers to the source-owned record that defines and executes one fixed acceptance case family through public seams. |
| Cleanup and signal requirements | Make every cleanup-addressable identity available to normal, error, and signal cleanup before use | The in-memory record of each run-owned page and pending blob identity that can be removed or cancelled through the public APIs | Record | learn public identity -> register identity -> use identity -> cleanup attempt -> public absence proof | run resource record | A run resource record refers to a cleanup-addressable public identity that the runner must release before terminal publication. |
| Public behavior oracle requirements | Separate observable facts from producer-authored judgments | The exact Framerail action bytes, Deepwell JSON-RPC results, WWS GET and HEAD responses, and adapter-issued event sequence captured for one case ID | Record | public request -> raw response -> independent verification -> case receipt | case observation | A case observation refers to raw public HTTP facts captured for one denominator case and contains no status or verdict field. |
| Clean source identity requirements | Bind a run to only the source files it actually loads | The sealed candidate commit, tree, FTML revision, and sorted hashes of one caller-supplied exact source manifest, collected only from a clean checkout | Record | exact loaded manifest -> clean checkout proof -> file hashes -> execution receipt | candidate source execution identity | A candidate source execution identity refers to a clean candidate source revision and the exact source manifest loaded by one runner. |
| No-replace publication requirements | Preserve auditable per-case and aggregate evidence | The canonical JSON records published only after exact reconciliation, cleanup absence, and stable post-run candidate identity, once per case and once for the aggregate | Record | verify observations -> prove cleanup -> prove stable identity -> publish cases -> publish aggregate | candidate case receipt | A candidate case receipt refers to one no-replace terminal record binding public observations, fixed inputs, candidate identity, cleanup proof, and source execution identity. |
