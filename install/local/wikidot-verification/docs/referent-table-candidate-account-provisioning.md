| Source | Purpose | Concrete Referent | Role | Ordering | Candidate Term | First-Use Definition |
| --- | --- | --- | --- | --- | --- | --- |
| Account A disposable-candidate requirement | Supply one existing Wikidot identity and a local candidate password without exposing either value in arguments or output | A mode-0600 JSON file containing the exact Wikidot numeric user ID, public username, local login identifier, local password, and the editable site slug | Value | private file -> validation -> local activation | `CandidateAccountProvisioningInput` | `CandidateAccountProvisioningInput` refers to the private values accepted only for one disposable candidate account activation. |
| Existing Deepwell activation interface | Create or update the candidate-local account while preserving the Wikidot identity | One operator-authenticated `user_activate_from_wikidot` request issued to the disposable candidate Deepwell endpoint | Means | validated private input -> operator request -> local account | `candidate account provisioning` | Candidate account provisioning refers to the operator action that activates the exact Wikidot identity only inside the disposable candidate. |
| Editable-site constraint | Limit the activated actor to the local site intended for authored comparison state | The role assignment that makes the activated account a member of `scpaiueouiuiuiui` without assigning roles on `scp-wiki`, `scp-jp`, or any other site | Event | local activation -> exact editable-site membership -> public login | `editable-site membership assignment` | Editable-site membership assignment refers to adding the activated user only to the configured editable candidate site. |
| Redacted operator evidence | Make provisioning auditable without publishing credentials or secret-bearing payloads | A JSON receipt containing the candidate identity hash, private-input SHA-256, exact numeric user ID, public username, login-identifier SHA-256, editable site slug, and assignment result | Record | activation -> membership assignment -> redacted publication | `CandidateAccountProvisioningReceipt` | `CandidateAccountProvisioningReceipt` refers to the secret-free record of one disposable candidate account activation. |
| Ordinary login regression | Prove the candidate account uses the normal public authentication path | A source-owned public `/-/login` behavior test that accepts the provisioned password and rejects a different password without a bypass endpoint | Purpose | provisioning -> correct password -> wrong password | `candidate account login contract` | Candidate account login contract refers to the public authentication behavior required after candidate-local activation. |
