# Open87 Basalt Wikidot users

This fixture imports only the three Wikidot identities needed by `scp-wiki:theme:basalt`. It is for an isolated disposable Open87 candidate database. It is not a global seed fixture.

The fixture binds site `scp-wiki`, slug `theme:basalt`, Wikidot page ID `1312334753`, the exact saved-source hash, the retained raw-live-DOM hash, and the frozen v9 report hash. The retained raw live DOM supplies all six page-content `printuser avatarhover` nodes. Four later sealed browser reports independently confirm the exact three user IDs, names, slugs, profile links, and the visible avatar matrix: two EstrellaYoshte, one Liryn, and two Placeholder McD. The third source occurrence is EstrellaYoshte inside the initially collapsed More memos block.

`runtime-state.json` is static state data accepted by the existing runtime-state fixture importer. A focused mock-transport test records the exact user import operations and parameters. This fixture does not enforce provenance, controller ownership, or disposable-runtime isolation; a caller-supplied `runtime-diff-*` string is not a safety boundary. A separate rollback-only Deepwell integration test reads these committed user rows and rerenders the seeded `theme:basalt` page. These tests do not apply the JSON fixture and rerender the page in one real disposable runtime, so that end-to-end step remains required before using this fixture as browser acceptance evidence. Do not add these rows to Deepwell seeding or change general user lookup behavior.

The hashes and identities in these files are evidence metadata checked for internal consistency; the generic runtime-state validator does not authenticate them. `retained-dom-shape.json` preserves one exact nested HTTPS printuser subtree per identity from the hash-bound raw DOM. The retained browser report records five rendered avatars, not the hidden third source occurrence. The six-node total is supported by the exact saved source and retained raw live DOM. The Rust test proves a full seeded page rerender after inserting the committed rows, but canonical Deepwell still serializes the avatar `src` as HTTP. It therefore does not claim HTTPS DOM parity, JSON-import-to-browser behavior, browser image fetch, or decoded avatar bytes.
