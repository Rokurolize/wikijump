{
  "schema_version": 1,
  "transforms": [
    {
      "id": "source-pane-rule-owned-by-marked-jp-layer",
      "reason": "The flattened imported stylesheet contains this source-pane rule as well as the canonical JP override. Remove the imported copy so the JP compatibility layer owns the rule in one place. The transform requires this exact upstream text exactly once and fails closed if the imported bytes change; on an upstream update, review the source-pane rule and either refresh the anchor or move the adaptation into the updated upstream layer.",
      "expected_matches": 1,
      "before": "/* Preserve code-reading affordances when the theme's serif body face reaches page action panes. */\n#action-area .page-source,\n#action-area textarea.editor-wikitext { font-family: \"SFMono-Regular\", Consolas, \"Liberation Mono\", monospace; }\n\n",
      "after": ""
    }
  ]
}
