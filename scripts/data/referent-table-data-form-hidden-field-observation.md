| Source | Purpose | Concrete Referent | Role | Ordering | Candidate Term | First-Use Definition |
| --- | --- | --- | --- | --- | --- | --- |
| `data-form-hidden-password-static-url-live-20260810.json`, hidden field run | Bind the observation to the retained authenticated Wikidot lifecycle evidence | The four sealed hidden-field controls over one generated create, save, edit, reload, and cleanup lifecycle | Record | artifact → configured value → absent user control → persisted value → excluded shapes | hidden-field lifecycle observation | The hidden-field lifecycle observation refers to the four sealed hidden-field controls in the retained scalar-field artifact. |
| Hidden field template `value: HIDDEN_CONFIGURED_ALPHA` | State which value becomes page data | The exact captured nonempty plain configured scalar is stored on create, restored on edit and reload, and displayed on the saved page | Value | artifact → configured value → absent user control → persisted value → excluded shapes | captured configured hidden value | Captured configured hidden value refers to the exact plain template-provided scalar that the retained lifecycle stores and displays. |
| Create and edit captures plus injected submission controls | Preserve the user-visible editing boundary | The generated editor has no hidden control or field fragment and declares empty `form-fields`; deliberately injected create or edit payload values do not replace the configured value | State | artifact → configured value → absent user control → persisted value → excluded shapes | hidden-field no-control state | Hidden-field no-control state refers to the generated editor state with no control or declared form field, while separately injected payload values are ignored by the observed save lifecycle. |
| Artifact scope and single-field fixture | Keep incomplete field shapes outside the observed contract | The documented numeric-looking `value: 1.0` example, empty, quoted, escaped, mixed-field, extra-property, direct-variable, ListPages selection and ordering, exact browser DOM/CSS beyond the absent control, and browser-transition shapes were not established | State | artifact → configured value → absent user control → persisted value → excluded shapes | remaining hidden-field shapes | Remaining hidden-field shapes refers to behavior outside the one observed nonempty single-field lifecycle. |
