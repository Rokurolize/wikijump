| Source | Purpose | Concrete Referent | Role | Ordering | Candidate Term | First-Use Definition |
| --- | --- | --- | --- | --- | --- | --- |
| User remediation request; `module-tagcloud` implementation | Record the executable saved-page behavior without claiming broader acceptance | A saved page containing an executable two-dimensional TagCloud whose public `page_view` changes after another page's tags change while `page_get` retains the save-time compiled artifact | State | saved page_view + article_view_cache_metadata seams and test anchor | saved TagCloud runtime freshness | Saved TagCloud runtime freshness refers to recomputing an executable two-dimensional TagCloud for a page view without rewriting its stored compiled artifact. |
| User remediation request; imported article cache policy | Record the cache consequence of tag-dependent rendering | Public `article_view_cache_metadata` returns no cache key for imported pages with executable two-dimensional TagCloud and returns a key for literal, effective 3D, and parser-invalid shapes | State | exact saved page_view + article_view_cache_metadata seams and test anchor | executable TagCloud cache fence | The executable TagCloud cache fence refers to denying imported article caching only when the canonical renderer can execute the authored two-dimensional TagCloud. |
| User remediation request; active mobile constraint | Prevent a database-only regression from being presented as browser acceptance | Browser-visible P7 TagCloud behavior has not been revalidated in this task because browser verification is explicitly deferred under the active mobile constraint | State | keep feature/property P7/browser acceptance explicitly blocked/deferred | browser acceptance deferred | Browser acceptance deferred refers to leaving P7 blocked until browser-visible behavior can be verified outside the active mobile constraint. |
