| Source | Purpose | Concrete Referent | Role | Ordering | Candidate Term | First-Use Definition |
| --- | --- | --- | --- | --- | --- | --- |
| `frontforum-custom-body-live-20260810.json` and its four sealed case IDs | Bind the observation to reproducible anonymous Wikidot output | The two positive and two negative anonymous `edit/PagePreviewModule` captures over one run-owned case matrix | Record | artifact → public interface → evaluated values → fail-closed controls → excluded inferences | FrontForum custom-body rendering observation | The FrontForum custom-body rendering observation refers to the four sealed anonymous PagePreview captures. |
| Positive canonical and alias/offset cases | State which runtime action the captures establish | A recognized body-bearing `FrontForum` consumes its exact closer and evaluates one body for each of the two captured one-row selections | Event | artifact → public interface → evaluated values → fail-closed controls → excluded inferences | custom-body evaluation | Custom-body evaluation refers to the exact closer consumption and one evaluated body observed in each captured one-row selection. |
| Documented variable matrix in the two positive cases | Name the values whose substitutions are established | The documented title, link, author, date, comments, category, description, content variables and their observed aliases | Value | artifact → public interface → evaluated values → fail-closed controls → excluded inferences | observed FrontForum variables | Observed FrontForum variables refers to the documented variables and aliases populated by the sealed positive captures. |
| Unknown-variable and malformed-category cases | Preserve literal and suppression behavior without widening ownership | An unknown variable remains literal, while a malformed category list renders the category error and does not leak or evaluate the custom body | State | artifact → public interface → evaluated values → fail-closed controls → excluded inferences | custom-body fail-closed controls | Custom-body fail-closed controls refers to the literal unknown variable and suppressed malformed-selector body states. |
| Artifact `ownership_boundary.forbidden_inferences` and one-row fixtures | Keep incomplete surfaces outside the observed contract | Raw or other-module closer ownership, multi-row or empty selection cardinality, body nesting and composition, date formats beyond `%Y-%m-%d`, feed and document-head behavior, `fixRelativeLinks`, private or deleted visibility, and forum mutation authority were not established by these captures | State | artifact → public interface → evaluated values → fail-closed controls → excluded inferences | remaining FrontForum surfaces | Remaining FrontForum surfaces refers to the explicitly unobserved behavior excluded from this observation. |
