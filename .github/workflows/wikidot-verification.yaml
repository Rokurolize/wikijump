name: '[wikidot] Verification tests'

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    name: Local-only policy
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Compatibility verification is local-only
        run: |
          printf '%s\n' \
            'Wikidot verification is intentionally disabled in GitHub Actions.' \
            'Run it in the maintained WSL workspace with the persistent identity-bound response cache.' \
            'A retained cache hit must perform zero external requests.'
