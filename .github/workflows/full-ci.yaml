name: Browser CI

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    name: Local-only policy
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Browser validation is local-only
        run: |
          printf '%s\n' \
            'Browser tests are intentionally disabled in GitHub Actions.' \
            'Run browser validation in the maintained WSL workspace so retained external-response evidence can be reused.'
